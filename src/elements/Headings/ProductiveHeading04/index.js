import React from "react";

export const ProductiveHeading04 = ({ heading, classes }) => {
  return (
    <h2 className={`productive-heading-04 ${classes ? classes : ""}`}>
      {heading}
    </h2>
  );
};
