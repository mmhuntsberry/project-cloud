// import React, { useState, useEffect } from "react";
// // import { InlineLoading } from "carbon-components-react";
// import { isEmail } from "validator";

// export const checkEmailConstraints = currentState => {
  

//   console.log(loading);

//   return <div>{loading && "HI"}</div>;

  //   /* { loading ?   (  <InlineLoading className="inline-loading" />
  //   ) : (
  //     <InlineLoading className="inline-loading" status="finished" />
  //     ) */
  // }

  // if (isEmail(email)) {
  //   return <InlineLoading className="inline-loading" status="finished" />;
  // }
  // if (!isEmail(email)) {
  //   return <InlineLoading className="inline-loading" status="error" />;
  // }
  // if (currentState.matches({ emailErr: "badFormat" })) {
  //   return <InlineLoading className="inline-loading" status="error" />;
  // }
  // if (currentState.context.email.length > 0) {
  //   return <InlineLoading className="inline-loading" />;
  // }
};
